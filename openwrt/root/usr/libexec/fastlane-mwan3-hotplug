#!/bin/sh

case "${ACTION:-}" in
	ifup|ifdown|connected|disconnected) ;;
	*) exit 0 ;;
esac

[ -x /usr/libexec/fastlane-awg-mwan3 ] || exit 0
/usr/libexec/fastlane-awg-mwan3 --interface fastlane_awg >/dev/null 2>&1 || true
exit 0
